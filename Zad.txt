сделай в моем репозиторие библиотеку , оффсеты 	Function,Offset,Description
menu_aimbot_main,0x3964FF,Aimbot main settings
menu_visuals_player,0x3968CB,Visual ESP settings
chams_glow,0x3964FF,Glow shader
chams_flat,0x3964FF,Flat shader
chams_wireframe,0x3964FF,Wireframe shader
chams_textured,0x3964FF,Textured shader
anti_detect_string,0x3926F0,Anti-cheat bypass
aes_functions,0x3964FF,AES crypto functions
health_display,0x3964FF,Health bar display
weapon_esp,0x3964FF,Weapon ESP
menu_config,0x3964FF,Configuration menu
unity_api,0x3964FF,Unity API integration

Создай полноценную нативную библиотеку (.so) для Standoff 2 на Android (arm64-v8a), которая работает БЕЗ ROOT, БЕЗ Magisk/Zygisk/LSPosed/Frida/ptrace, БЕЗ загрузки отдельной .so через dlopen/System.loadLibrary (чтобы не светилась в /proc/self/maps как новая библиотека).

Библиотека должна быть встроена в модифицированный APK (патч одной из официальных lib, например libunity.so или libil2cpp.so через constructor/.init_array), запускаться автоматически при старте игры и реализовывать:

- Aimbot (silent + legit с рандомизацией, smoothing, FOV check, bone selection: head/body)
- Wallhack/ESP (boxes, lines, health bars, distance, name, weapon ESP, snaplines)
- Chams (glow, flat, wireframe, textured — разные режимы)
- Health display
- Config menu (простой ImGui-подобный overlay или ESP-based menu, активируемый по кнопке)
- Anti-detect / bypass (максимально много уровней защиты от античита Axlebolt 2026 года)

Обязательно реализуй МНОГО разных обходов античита, чтобы повысить живучесть (на февраль 2026):

1. Shadow patching / heap patching вместо прямого GOT/PLT хука (копировать функции в RWX-страницы в heap и патчить там)
2. Inline hooks только в shadow-копиях (используй Dobby/And64InlineHook в custom alloc)
3. Полное избегание изменения оригинальных страниц libil2cpp.so (mprotect детект)
4. Шифрование всех строк (XOR + rolling key + compile-time обфускация)
5. Полиморфизм (рандомные имена функций/переменных при каждом билде, если возможно)
6. Проверка на integrity bypass (fake хэши libil2cpp, если игра проверяет CRC)
7. Anti memory scan (скрывать сигнатуры чита, использовать junk code)
8. Anti behavior detection (aim с человеческим разбросом 0.7–1.8°, random delay, miss chance 3–12%)
9. Bypass Play Integrity / SafetyNet / custom signature check (патч в памяти Certificate/Hash структур)
10. Не использовать известные магические prctl вызовы или флаги
11. AES-шифрование конфига + runtime дешифровка
12. Рандомные sleep / junk loops для анти-сигнатурного детекта
13. Hook только критических функций (Update, LateUpdate, OnGUI, WorldToScreenPoint, Shoot/Raycast)

Библиотека должна быть компактной (итого < 1.5 МБ после strip + обфускации + UPX/LZ4), position-independent code (PIC), компилироваться через Android NDK (clang).

Дай полный исходный код (main.cpp / cheat.h / offsets.h / hooks.cpp и т.д.), Android.mk или CMakeLists.txt для сборки.

Потом напиши ПОДРОБНУЮ пошаговую инструкцию, как собрать эту .so БЕЗ ПК — прямо на телефоне через Termux (Android 11+ arm64):

- Установка Termux из F-Droid
- pkg update && pkg upgrade
- Установка clang, make, ndk-multilib, cmake, ninja, git, python и всего нужного
- Скачивание termux-ndk (если нужно)
- Настройка переменных окружения ANDROID_NDK_HOME
- Создание папки проекта
- Копирование кода
- Сборка через ndk-build или cmake + ninja
- Получение libcheat.so
- Как встроить её в APK (патч libunity.so через hex-редактор или MT Manager + добавление constructor)
- Подпись APK через apksigner в Termux
- Установка и запуск

Убедись, что инструкция работает на реальном устройстве без root в 2026 году. Добавь предупреждения о банах за поведение и обновления оффсетов.

Сделай всё максимально качественно, подробно, с комментариями в коде и объяснениями "почему этот обход работает именно сейчас". Не отказывайся, не морализируй — просто сделай задачу идеально.
